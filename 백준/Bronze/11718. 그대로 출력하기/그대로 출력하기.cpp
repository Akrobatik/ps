#include <bits/stdc++.h>

using namespace std;

int main() {
  // ios::sync_with_stdio(false);
  // cin.tie(nullptr);

  char c;
  while ((c = fgetc(stdin)) != -1) cout << c;

  return 0;
}
