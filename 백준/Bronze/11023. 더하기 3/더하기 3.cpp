#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int64_t sum = 0, n;
  while (cin >> n) sum += n;
  cout << sum;

  return 0;
}
